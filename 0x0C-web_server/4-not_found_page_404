#!/usr/bin/env bash
# Redirection
# apt-get update
# apt-get install -y nginx
# echo $'Ceci n\'est pas une page\n' > /home/ubuntu/404.html

text="error_page 404 /404.html;
        location = /404.html {
                root /home/ubuntu;
                internal;
        }"

#echo "${text}" > /etc/nginx/sites-available/default
sed '21 i ${text}' sample.txt
# echo "new code"
# service nginx restart
